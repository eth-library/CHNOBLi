# Kept in sync with the [project.dependencies] table in pyproject.toml.
lxml>=6.1.0
germalemma>=0.1.3
requests>=2.33.1
flair==0.15.1
gensim>=4.4.0
orjson>=3.10.18
pytest>=9.0.3
rich>=15.0.0
pydantic-settings>=2.14.0
# No longer necessary once we have the API running
pymilvus[bulk-writer]>=2.6.3,<3

# Optional: only required to generate embeddings locally (EMBEDDINGS_ENDPOINT unset).
# Install with `pip install "CHNOBLi[local-embeddings]"` or uncomment the line below.
# embedding-engine @ git+https://github.com/eth-library/CHNOBLi-embedding-engine.git@v1.0.1
